version: "3.1"

rules:
  - rule: Fallback rule
    steps:
      - intent: nlu_fallback
      - action: utter_fallback
  - rule: Respond to geographic feature queries
    steps:
    - intent: ask_for_geographic_features
    - action: action_list_geographic_features